import { expenseAppCheckRegistryPort } from '../adapters/consumer/expense-app/work-registry/check-registry-adapter.ts';

/** Production CheckRegistry composition for the expense-app consumer. */
export const productionCheckRegistryPort = expenseAppCheckRegistryPort;
