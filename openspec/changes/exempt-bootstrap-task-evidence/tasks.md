## 1. Managed Baseline

- [x] 1.1 Refresh the generated semantic handoff for this active change and prove the managed-document baseline is clean before implementation begins.

## 2. Canonical-Evidence Epoch

- [ ] 2.1 Follow RED -> GREEN -> REFACTOR to derive each change's canonical-evidence epoch from its earliest canonical plan commit and exempt only pre-epoch task completions from per-task commit evidence, identically in live archive eligibility and CI archive replay, while canonical-era completions keep failing closed.
