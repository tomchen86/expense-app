## 1. Replace the Agent Skill Surface

- [ ] 1.1 Add a RED repository contract for the approved OpenSpec skill mirrors and Spectra exclusions; delete the ten approved Spectra skill files, add the two byte-identical OpenSpec mirrors, align `AGENTS.md`, `.agents/README.md`, and the Roadmap with the historical-only Spectra boundary, then pass the registered non-database checks.
