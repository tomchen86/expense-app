## 1. Archive-Stable Handoff

- [ ] 1.1 Add failing multi-change archive-selection coverage, implement safe archived-contract and branch-bound handoff selection, render the archive-stable generated projection, and verify normal hooks from tracked state.
