## 1. Canonical ignored-directory records

- [ ] 1.1 Add RED-first strict-path and ignored nested-repository fingerprint/validation regressions, implement the source-specific Git ignored-record adapter, and pass all registered workflow-engine checks.
