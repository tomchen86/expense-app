When user select a category in the add/edit page, the page color will change.
The color of category should also be seen in the add/edit page category select drop down.
When user delete a category it won't affect to the history data which mean the history data remain the same, but user can not use it anymore.(guarentee)

## migrate api test to vitest to better support ESM
