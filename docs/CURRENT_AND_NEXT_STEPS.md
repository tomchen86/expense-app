# Current and Next Steps

This generated handoff contains semantic project state only. Its sources are tracked OpenSpec change records and structured issue data.

## Current Change

`establish-investigation-first-planning`

## Current Task

`6.5` — Follow RED -> GREEN -> REFACTOR to complete the resumable `workflow propose` happy path through lifecycle-owned background execution of durable blind-survey and exact-PlanReview work: handle the structured investigation-exemption path without manufacturing scan/WHY evidence; keep the code-owned propose-policy binding on authorization/evidence nodes while binding each ordinary or granted provider request to the pinned raw adapter-policy digest and lowered limits; reserve and consume grants around exact content admission; acquire a fenced invocation lease when a provider is actually used; admit only the fixed built-in provider result, caller-supplied typed result, or direct-human signed attestation allowed by the assignment; validate and persist the semantic result together with honest orchestration, achieved independence, unchanged-governed-projection when applicable, executable identity, containment residual, and runtime assurance; durably classify launch or admission failure without publishing success; and resume from the exact stored result through challenge disposition, planning authorization, conflict recovery, and typed terminal output using the already-shared live validator. Prove the full provider path on both SHA-1 and SHA-256 Git object formats while retaining deterministic fake-driver injection and credential-free registered tests.

## Next Task

`6.6` — Follow RED -> GREEN -> REFACTOR to make CI and local archive call the same pure validators, including aggregate collaboration-grant use-set uniqueness and investigation-exemption eligibility; resolve immutable governing generations from managed Git history; preserve actual orchestration and achieved independence without consulting local reservation state or upgrading soft claims; and replay legacy plus non-default v2 plans without runtime objects or provider credentials.

## Current Focus

Follow RED -> GREEN -> REFACTOR to complete the resumable `workflow propose` happy path through lifecycle-owned background execution of durable blind-survey and exact-PlanReview work: handle the structured investigation-exemption path without manufacturing scan/WHY evidence; keep the code-owned propose-policy binding on authorization/evidence nodes while binding each ordinary or granted provider request to the pinned raw adapter-policy digest and lowered limits; reserve and consume grants around exact content admission; acquire a fenced invocation lease when a provider is actually used; admit only the fixed built-in provider result, caller-supplied typed result, or direct-human signed attestation allowed by the assignment; validate and persist the semantic result together with honest orchestration, achieved independence, unchanged-governed-projection when applicable, executable identity, containment residual, and runtime assurance; durably classify launch or admission failure without publishing success; and resume from the exact stored result through challenge disposition, planning authorization, conflict recovery, and typed terminal output using the already-shared live validator. Prove the full provider path on both SHA-1 and SHA-256 Git object formats while retaining deterministic fake-driver injection and credential-free registered tests.

## Known Blockers

- `ISS-003` — Activate workflow-assurance branch rules
- `ISS-205` — Recover the web application source boundary

## References

- [Roadmap](ROADMAP.md)
- [Change records](../openspec/changes/)
- [Base specifications](../openspec/specs/)
- [Issue log](ISSUE_LOG.md)
- [System architecture](architecture/ARCHITECTURE.md)
